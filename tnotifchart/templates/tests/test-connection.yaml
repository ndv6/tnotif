apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tnotifchart.fullname" . }}-test-connection"
  labels:
    {{- include "tnotifchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tnotifchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
