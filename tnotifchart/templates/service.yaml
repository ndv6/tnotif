apiVersion: v1
kind: Service
metadata:
  name: {{ include "tnotifchart.fullname" . }}
  labels:
    {{- include "tnotifchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tnotifchart.selectorLabels" . | nindent 4 }}
